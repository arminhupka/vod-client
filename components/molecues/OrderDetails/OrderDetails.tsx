import { Paper } from "@mui/material";
import { ReactElement } from "react";

import { StyledWrapper } from "./OrderDetails.styles";

const OrderDetails = (): ReactElement => (
  <StyledWrapper>
    <Paper>
      <p>hello</p>
    </Paper>
  </StyledWrapper>
);

export default OrderDetails;
